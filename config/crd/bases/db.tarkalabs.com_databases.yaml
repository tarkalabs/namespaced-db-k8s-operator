---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: databases.db.tarkalabs.com
spec:
  group: db.tarkalabs.com
  names:
    kind: Database
    listKind: DatabaseList
    plural: databases
    singular: database
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Database is the Schema for the databases API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: DatabaseSpec defines the desired state of Database
            properties:
              instanceName:
                description: The database instance name
                minLength: 1
                type: string
              name:
                description: The name of the database to be created inside the database
                  instance
                minLength: 1
                type: string
              secret:
                description: The secret name containing the database username. The
                  password will be autogenerated and stored by the controller
                minLength: 1
                type: string
            required:
            - instanceName
            - name
            - secret
            type: object
          status:
            description: DatabaseStatus defines the observed state of Database
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
